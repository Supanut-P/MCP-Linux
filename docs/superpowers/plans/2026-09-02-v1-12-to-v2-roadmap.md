# Baitonghub-Linux-mcp v1.12–v2.0 Additive Development Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Continue Baitonghub-Linux-mcp from the locally verified v1.11.0 headless release to a more useful Linux operations gateway through bounded, additive features, while keeping v1 compatibility and the existing registered-root, ownership, confirmation, audit, and secret-redaction boundaries.

**Architecture:** Ship one independently testable capability per minor release. Reuse the existing `LinuxCommandRunner`, `LinuxObservabilityBackend`, `WorkspaceIndexService`, `RemoteFleetBackend`, `ActivityTracker`, durable task stores, and generated tool-contract checks. The default server remains Linux headless and v1-compatible; no release in this plan grants arbitrary root filesystem access, arbitrary remote shell, plaintext secrets, or unowned process control.

**Tech Stack:** TypeScript, Node.js 24, MCP SDK `2026-07-28`, Zod, SQLite, systemd/journalctl, procfs, OpenSSH, pnpm 10.15.0, Vitest, Ubuntu 24.04 x64, DEB and Linux x64 tar packaging.

---

## Baseline and rules

- Current implementation is the locally and Ubuntu-verified v1.25.0 checkpoint on `codex/v0.2.0-observability`; the packaged source commit is `beb3c7e8f61bc033a53178a02a3b26417620b880`. No push, tag, or GitHub release has been performed. The prior v1.11.0 baseline was commit `6475e794eec3a112190aa2d1ef972bcaef1e76a8`.
- Ubuntu VM103 proof is the release authority: full unit suite passed (17 projects; capabilities 24 files/121 tests, mcp-server 49 files/265 tests, CLI 7 files/18 tests), integration 3/3, packaging tests 15/15, release gate 10/10, catalog/contract checks passed at 225 tools, v1 acceptance 5/5, and packaged STDIO smoke passed for DEB and tar (including `policy_explain`, `task_events`, `task_history`, `diagnostics_snapshot`, `remote_fleet_diff`, `audit_query`, the `workspace_snapshot` manifest, `release_verify`, and `environment_preflight`). DEB/tar package inspection plus provenance/upgrade preflight passed. The VM source export is intentionally Git-free for public hygiene; branding and diff checks were also verified after normalizing the test export.
- The seven-day soak is explicitly waived. Every release note must say that it has not run and must not claim production-soak evidence.
- The stale exact-order expectation in `packages/mcp-server/src/tool-registry.test.ts` was corrected as part of this checkpoint; `remote_fleet`, `service_logs`, and generated catalog/contract files are synchronized.
- Every minor release follows the same loop: write a failing test, implement the smallest bounded provider, run focused tests, run the full Ubuntu gate and packaged smoke, update docs/version/provenance, then stop at the human gate before external push/tag/release.

## Release map

| Version | User value | Public change | Hard acceptance gate |
| --- | --- | --- | --- |
| v1.12.0 | Follow one systemd service log stream without shell commands | Add read-only `service_logs` with fixed unit validation, byte/line caps, and opaque cursor | Cursor cannot escape its unit, output is capped/sanitized, and missing journalctl returns structured unavailable |
| v1.13.0 | See whether the gateway or host is overloaded | Add read-only `runtime_metrics` snapshot for host, MCP runtime, and owned tasks | No command-line/env/secret disclosure; deterministic bounded JSON and truthful missing-provider state |
| v1.14.0 | Detect workspace changes between agent turns | Add read-only `workspace_changes` snapshot/diff over the existing index/watch queue | Only registered workspace paths, bounded event retention, opaque cursor owner-isolated, no file contents returned |
| v1.15.0 | Check many registered hosts in one operation | Extend `remote_fleet` with bounded `snapshot` and per-host progress, without new remote authority | 1–20 registered hosts, max four sessions, per-host sanitized errors, deterministic partial result |
| v1.16.0 | Run the same server as a small core, operator, or fleet surface | Add opt-in additive server profiles; default remains v1-compatible full headless profile | `tools/list` filtering is explicit, stable, and cannot elevate permissions or reveal secret metadata |
| v1.17.0 | Understand what the gateway did without exposing secrets | Add bounded `audit_query` for redacted, cursor-based activity summaries | Results contain counts/result codes and sanitized tool/workspace identifiers only; cursor is owner-isolated and output is capped |
| v1.18.0 | Compare a workspace safely before/after an agent run | Add read-only `workspace_snapshot` manifest with bounded metadata and optional streaming hash | Only registered roots are traversed; special files/symlink escapes are rejected; manifest and hash output are deterministic and capped |
| v1.19.0 | Follow long-running work after a reconnect | Add `task_events` cursor stream for durable task lifecycle events | Events are owner-isolated, bounded, resumable, and never include command lines, environments, secrets, or raw provider stderr |
| v1.20.0 | See why an operation is unavailable before retrying | Add read-only `policy_explain` for profile, permission, consent, dependency, and registered-root decisions | Explanation is sanitized and advisory only; it cannot grant authority or bypass confirmation |
| v1.21.0 | Find prior work without reading raw logs | Add bounded `task_history` over retained owned task summaries with state/age/workspace-hash filters | History is owner-scoped, retention-bounded, and excludes command, path, output, environment, and secret metadata |
| v1.22.0 | Diagnose one incident with one safe snapshot | Add read-only `diagnostics_snapshot` combining health, runtime pressure, audit counts, and dependency readiness | Fixed sections, sanitized reasons, deterministic byte cap, and no hidden topology or credential disclosure |
| v1.23.0 | Detect remote configuration drift | Add `remote_fleet_diff` over two bounded registered-host snapshots | Read-only comparison only; host IDs are registered aliases, per-host errors are sanitized, and no new remote authority is granted |
| v1.24.0 | Verify what was installed before an upgrade | Add offline package provenance/checksum verification with explicit artifact paths | No download or install side effect; signature/checksum failures are fail-closed and evidence is reproducible |
| v1.25.0 | Know whether this headless host is ready | Add sanitized runtime, display-server, and dependency preflight over existing health providers | No hostname, command, path, secret, or mutation data; missing providers are truthful and bounded |
| v2.0.0 decision | Only address a proven incompatibility | Separate v2 contract only when real client/deprecation evidence requires breaking change | Frozen v1 fixture, new v2 fixture, migration/rollback proof, and explicit approval |

## Common release gate

Run on Ubuntu 24.04 x64 from a clean checkout:

```bash
corepack pnpm@10.15.0 install --frozen-lockfile
corepack pnpm@10.15.0 rebrand:check
corepack pnpm@10.15.0 lint
corepack pnpm@10.15.0 typecheck
corepack pnpm@10.15.0 test
corepack pnpm@10.15.0 test:integration
corepack pnpm@10.15.0 test:packaging
corepack pnpm@10.15.0 test:release-gate
corepack pnpm@10.15.0 docs:tools:check
corepack pnpm@10.15.0 contract:v1
corepack pnpm@10.15.0 test:v1
git diff --check
```

Expected: all commands exit `0`; Linux durable-process tests execute instead of skipping; generated tool catalog and frozen v1 fixture match the live registry.

Build and inspect artifacts:

```bash
corepack pnpm@10.15.0 package:linux:headless
node scripts/generate-release-provenance.mjs dist
bash scripts/verify-linux-package.sh dist
bash scripts/verify-upgrade-rollback.sh dist
```

Expected: DEB and Linux x64 tar pass checksum, SBOM, provenance, install/upgrade/rollback/uninstall, STDIO, HTTP MCP, and forbidden-file checks. No `.exe`, `.cmd`, `.bat`, `.ps1`, Electron GUI binary, PowerShell bridge, or Windows OCR helper may be present.

---

### Task 0: Close v1.11 contract hygiene

**Files:**
- Modify: `packages/mcp-server/src/tool-registry.test.ts`
- Verify: `docs/architecture/TOOL_CONTRACT.md`
- Verify: `tests/fixtures/tool-contract-v1.json`
- Verify: `docs/architecture/TOOL_CONTRACT.md` (generated ToolRegistry catalog block)

- [x] **Step 1: Fix the deterministic order assertion**

Make the expected list derive the same concrete capability order as `ToolRegistry.list()`, with `remote_fleet` in its live capability position. Do not delete a runtime tool to satisfy the test.

- [x] **Step 2: Regenerate and test the contract**

```bash
corepack pnpm@10.15.0 docs:tools
corepack pnpm@10.15.0 contract:v1:write
corepack pnpm@10.15.0 --filter @baitonghub-linux-mcp/mcp-server test -- src/tool-registry.test.ts
corepack pnpm@10.15.0 docs:tools:check
corepack pnpm@10.15.0 contract:v1
```

Expected: registry tests and both generated checks pass with one stable tool order.

- [x] **Step 3: Include the hygiene change in the v1.12 checkpoint**

```bash
git add packages/mcp-server/src/tool-registry.test.ts docs/architecture/TOOL_CONTRACT.md tests/fixtures/tool-contract-v1.json
git commit -m "test: synchronize v1 tool order"
```

---

### Task 1: v1.12.0 bounded service logs

**Files:**
- Create: `packages/capabilities/src/service-logs-backend.ts`
- Create: `packages/capabilities/src/service-logs-backend.test.ts`
- Modify: `packages/capabilities/src/index.ts`
- Modify: `packages/mcp-server/src/tools/schemas.ts`
- Modify: `packages/mcp-server/src/tools/capability-tools.ts`
- Modify: `packages/mcp-server/src/tool-registry.ts`
- Modify: `packages/mcp-server/src/tool-registry.test.ts`
- Modify: `packages/mcp-server/src/tools/tool-types.ts`
- Modify: `apps/cli/src/runtime/stdio-mcp-runtime.ts`
- Modify: `docs/mcp/MCP_TASKS.md`
- Modify: `HEADLESS_LINUX.md`
- Modify: `docs/architecture/TOOL_CONTRACT.md`
- Modify: `tests/fixtures/tool-contract-v1.json`
- Create: `RELEASE_NOTES_v1.12.0.md`

- [x] **Step 1: Write the failing schema and backend tests**

Use this public shape:

```ts
export const serviceLogsCapabilitySchema = z.object({
  operation: z.enum(['read', 'tail']).default('read'),
  unit: z.string().trim().regex(/^[A-Za-z0-9_.@:-]{1,256}\\.service$/),
  cursor: z.string().regex(/^[A-Za-z0-9_-]{1,512}$/).optional(),
  lines: z.number().int().min(1).max(500).default(100),
  maxBytes: z.number().int().min(1_024).max(256 * 1024).default(256 * 1024),
}).strict();
```

Tests must prove that invalid unit/cursor/limits are rejected, journalctl receives an argv array with `shell:false`, records are sanitized, output never exceeds `maxBytes`, and a cursor from unit A cannot be reused for unit B.

- [x] **Step 2: Implement the fixed-unit reader**

Run only the resolved `journalctl` executable with `--no-pager --output=json --utc -u <unit> -n <lines>`. Encode `{ unit, timestampMs, sequence }` as the opaque base64url cursor. Decode only numeric values within the accepted range, derive `--since @<seconds>`, and never pass an unvalidated cursor string to a subprocess. Return `{ entries, nextCursor, truncated, provider: 'journalctl', unit }` with the existing redaction helper. Map provider failure to `CAPABILITY_UNAVAILABLE` and cancellation to the existing timeout result.

- [x] **Step 3: Register health and tool metadata**

Advertise `service_logs` only on Linux, mark it `READ`/`readOnlyHint: true`, report `journalctl` in `health.missingDependencies`, and do not expose arbitrary journal predicates, shell fragments, boot IDs, or raw provider stderr.

- [x] **Step 4: Run focused tests and regenerate contracts**

```bash
corepack pnpm@10.15.0 --filter @baitonghub-linux-mcp/capabilities test -- src/service-logs-backend.test.ts
corepack pnpm@10.15.0 --filter @baitonghub-linux-mcp/mcp-server test -- src/tool-registry.test.ts
corepack pnpm@10.15.0 docs:tools
corepack pnpm@10.15.0 contract:v1:write
```

Expected: focused tests pass and the live catalog gains exactly one tool.

- [x] **Step 5: Package-test, version, and stop at the gate**

```bash
corepack pnpm@10.15.0 set-version 1.12.0
git add packages apps HEADLESS_LINUX.md docs/mcp/MCP_TASKS.md docs/architecture/TOOL_CONTRACT.md tests/fixtures/tool-contract-v1.json RELEASE_NOTES_v1.12.0.md package.json pnpm-lock.yaml
git commit -m "feat: add bounded service logs"
```

Run the common release gate and packaged `service_logs` read/tail smoke on VM103. Do not push/tag/release without approval.

**v1.12 acceptance:** a client can read and continue one registered systemd service stream with bounded sanitized output, without generic shell authority or false success when journalctl is unavailable.

**v1.12 proof:** VM103 Ubuntu 24.04 x64 ran capabilities `24 files / 121 passed`, mcp-server `37 files / 208 passed`, integration `3/3`, packaging `15/15`, release gate `10/10`, catalog/contract/rebrand checks, and package inspection. Built artifact hashes are recorded in `dist/v1.12.0-ubuntu-vm103/`. The seven-day soak was waived and no external push/tag/release was performed.

---

### Task 2: v1.13.0 runtime metrics snapshot

**Files:**
- Create: `packages/mcp-server/src/runtime-metrics-service.ts`
- Create: `packages/mcp-server/src/runtime-metrics-service.test.ts`
- Modify: `packages/mcp-server/src/tools/schemas.ts`
- Modify: `packages/mcp-server/src/tools/capability-tools.ts`
- Modify: `packages/mcp-server/src/tool-registry.ts`
- Modify: `packages/mcp-server/src/tools/tool-types.ts`
- Modify: `packages/mcp-server/src/activity-tracker.ts`
- Modify: `apps/cli/src/runtime/stdio-mcp-runtime.ts`
- Modify: `docs/architecture/TOOL_CONTRACT.md`
- Modify: `tests/fixtures/tool-contract-v1.json`
- Modify: `HEADLESS_LINUX.md`
- Create: `RELEASE_NOTES_v1.13.0.md`

- [x] **Step 1: Write bounded metric tests**

Use this input:

```ts
{ operation: 'snapshot', scopes: ['host', 'runtime', 'tasks'] }
```

Assert the result contains only numeric host load/memory/uptime, MCP request totals/active count/revision, and owned-task counts by state. Assert it contains no hostname, command line, environment, path, client ID, actor ID, task token, secret, or raw error. Unknown scopes and more than three scopes fail with `INVALID_INPUT`.

- [x] **Step 2: Implement `RuntimeMetricsService`**

Read host values from Node `os`, runtime values from `ActivityTracker` and the existing shared activity snapshot, and task counts from existing shell/remote task ports. Cap serialized output at 64 KiB and return `CAPABILITY_UNAVAILABLE` when an optional task port is not wired; never shell out or read arbitrary `/proc/<pid>/cmdline`.

- [x] **Step 3: Wire metadata and tests**

Register `runtime_metrics` as a read-only tool. Its health provider is `node:os+activity`; it is available in headless mode and does not require display consent. Add registry filtering tests proving it is absent only when the service is not wired.

- [x] **Step 4: Verify and release locally**

```bash
corepack pnpm@10.15.0 --filter @baitonghub-linux-mcp/mcp-server test -- src/runtime-metrics-service.test.ts src/tool-registry.test.ts
corepack pnpm@10.15.0 typecheck
corepack pnpm@10.15.0 docs:tools
corepack pnpm@10.15.0 contract:v1:write
corepack pnpm@10.15.0 set-version 1.13.0
git add packages apps HEADLESS_LINUX.md docs/architecture/TOOL_CONTRACT.md tests/fixtures/tool-contract-v1.json RELEASE_NOTES_v1.13.0.md package.json pnpm-lock.yaml
git commit -m "feat: add runtime metrics snapshot"
```

Run the common Ubuntu/package gate and stop before external release mutation.

**v1.13 acceptance:** an operator can distinguish host pressure, active MCP work, and owned task backlog from one bounded read-only response without learning credentials or process command lines.

**v1.13 proof:** local runtime-metrics/registry tests pass (`37/37` focused), full mcp-server tests pass (`38 files / 211 passed / 1 Windows-only skip`), lint and typecheck pass, and the Ubuntu VM103 gate passes (`17 projects / 212 tests`, integration `3/3`, packaging `15/15`, release gate `10/10`, catalog/contract/rebrand). Package inspection and upgrade/rollback preflight pass; latest VM artifacts are recorded under `dist/v1.13.0-ubuntu-vm103/` with tar SHA-256 `e1c7d3cf75a892c2fd86b6e1dc7ebab67cbdad8a204939d04366dbc512b5c053` and DEB SHA-256 `e3fe4967ce5fb2877861c98f0cc402922ba5e6eadf7743aa57fb8548845c69e5`. The seven-day soak remains waived and no external push/tag/release is performed.

---

### Task 3: v1.14.0 registered-workspace change feed

**Files:**
- Modify: `packages/application/src/workspace-index-queue.ts`
- Modify: `packages/application/src/workspace-index.ts`
- Create: `packages/application/src/workspace-changes-service.ts`
- Create: `packages/application/src/workspace-changes-service.test.ts`
- Modify: `packages/application/src/index.ts`
- Modify: `packages/mcp-server/src/tools/schemas.ts`
- Modify: `packages/mcp-server/src/tools/workspace-index-tools.ts`
- Modify: `packages/mcp-server/src/tool-registry.ts`
- Modify: `packages/mcp-server/src/tools/tool-types.ts`
- Modify: `apps/cli/src/runtime/stdio-mcp-runtime.ts`
- Modify: `docs/architecture/TOOL_CONTRACT.md`
- Modify: `tests/fixtures/tool-contract-v1.json`
- Modify: `HEADLESS_LINUX.md`
- Create: `RELEASE_NOTES_v1.14.0.md`

- [x] **Step 1: Add queue event assertions**

Expose a bounded, non-content event record `{ sequence, relativePath, kind, observedAt }`. Tests must prove duplicate paths coalesce, sequence values are monotonic, queue retention is at most 200 events, and events from workspace A are never returned for workspace B.

- [x] **Step 2: Implement owner-scoped snapshot/diff**

Add `workspace_changes` with:

```ts
z.discriminatedUnion('operation', [
  z.object({ operation: z.literal('snapshot'), workspaceId: workspaceIdSchema, maxEvents: z.number().int().min(1).max(200).default(50) }).strict(),
  z.object({ operation: z.literal('diff'), workspaceId: workspaceIdSchema, afterSequence: z.number().int().min(0), maxEvents: z.number().int().min(1).max(200).default(50) }).strict(),
])
```

Require a registered workspace, normalize relative paths, omit file contents and absolute roots, and return `truncated` when events were evicted. A missing watcher returns a truthful `watcher_not_running` state instead of starting one implicitly.

- [x] **Step 3: Run Linux watcher tests**

Use a temporary registered root on Ubuntu, start the existing watcher, create/modify/delete files, call snapshot/diff, and assert only relative paths and event kinds are returned. Add a symlink-escape regression proving the event path cannot leave the workspace root.

- [x] **Step 4: Regenerate, version, package, and stop**

```bash
corepack pnpm@10.15.0 --filter @baitonghub-linux-mcp/application test -- src/workspace-changes-service.test.ts
corepack pnpm@10.15.0 --filter @baitonghub-linux-mcp/mcp-server test -- src/tool-registry.test.ts
corepack pnpm@10.15.0 docs:tools
corepack pnpm@10.15.0 contract:v1:write
corepack pnpm@10.15.0 set-version 1.14.0
git add packages apps HEADLESS_LINUX.md docs/architecture/TOOL_CONTRACT.md tests/fixtures/tool-contract-v1.json RELEASE_NOTES_v1.14.0.md package.json pnpm-lock.yaml
git commit -m "feat: add workspace change feed"
```

Run the full Ubuntu gate and packaged change-feed smoke. Stop before external release mutation.

**v1.14 acceptance:** a client can resume a bounded owner-scoped path-change feed for a registered workspace without receiving file contents, absolute paths, or events from another workspace.

**v1.14 proof:** application focused tests pass (`76/76`), full root unit tests pass locally with the existing Linux-only suites skipped on Windows, lint/typecheck pass, and catalog/contract/rebrand checks are synchronized at `217` tools. On Ubuntu 24.04 VM103, clean-build typecheck, lint, full unit suite (`17 projects`; mcp-server `38 files / 213 passed`, capabilities `121/121` including durable-shell, CLI `17/17`), integration `3/3`, packaging tests `7/7`, release gate `10/10`, and catalog check passed. DEB/tar package inspection and provenance/upgrade-rollback preflight passed; packaged smoke reported `211` provider-filtered tools. Artifacts are retained in `dist/v1.14.0-ubuntu-vm103/` with tar SHA-256 `43550e71b40c5eedc26f3278cfe4253800bf6085b27874b67c2058328cb3ee65` and DEB SHA-256 `5a6cc15b512b6ad6ba2b90cf02c73943f0fb7bd1657efd1165c627a23ab6fdba`, bound to commit `0f772285730df5a768cf2fe87d06e297e62a2b4c`. Public-repo hygiene/rebrand remain local-Git checks because the VM source snapshot intentionally has no `.git`; the seven-day soak remains waived and no external push/tag/release was performed.

---

### Task 4: v1.15.0 remote fleet snapshot

**Files:**
- Modify: `packages/capabilities/src/remote-host-backend.ts`
- Modify: `packages/capabilities/src/remote-host-backend.test.ts`
- Modify: `packages/mcp-server/src/tools/schemas.ts`
- Modify: `packages/mcp-server/src/tools/capability-tools.ts`
- Modify: `packages/mcp-server/src/tool-registry.ts`
- Modify: `packages/mcp-server/src/remote-fleet-runtime.ts`
- Create: `packages/mcp-server/src/remote-fleet-runtime.test.ts`
- Modify: `packages/mcp-server/src/tools/tool-types.ts`
- Modify: `apps/cli/src/runtime/stdio-mcp-runtime.ts`
- Modify: `docs/linux/REMOTE_FLEET.md`
- Modify: `docs/architecture/TOOL_CONTRACT.md`
- Modify: `tests/fixtures/tool-contract-v1.json`
- Modify: `HEADLESS_LINUX.md`
- Create: `RELEASE_NOTES_v1.15.0.md`

- [x] **Step 1: Write snapshot validation tests**

Extend the existing schema with `operation: 'snapshot'`, optional fixed `unit`, optional registered relative `path`, and `maxParallel: 1..4`. Prove duplicate/unknown host IDs fail before any SSH connection, the maximum host count is 20, and a single host failure does not erase other host results.

- [x] **Step 2: Implement bounded aggregation**

For each registered host call only existing `health`, `inventory`, and `service-status` operations. Use a four-session semaphore, per-host timeout, deterministic host-ID order, and a 500-entry/256 KiB cap per host. Return `{ hosts, completed, failed, truncated, maxParallel }`; errors contain only sanitized result codes and durations. Never accept hostname, username, key path, raw command, or secret input.

- [x] **Step 3: Add remote audit and health tests**

Audit only host alias, remote workspace ID when applicable, operation, result code, and duration. Add tests for pinned-key failure, timeout, cancellation, and partial results. Ensure `remote_fleet` remains gated on `remote_host` advertisement and cannot grant local-full authority.

- [x] **Step 4: Verify and version locally**

```bash
corepack pnpm@10.15.0 --filter @baitonghub-linux-mcp/capabilities test -- src/remote-host-backend.test.ts
corepack pnpm@10.15.0 --filter @baitonghub-linux-mcp/mcp-server test -- src/remote-fleet-runtime.test.ts src/tool-registry.test.ts
corepack pnpm@10.15.0 docs:tools
corepack pnpm@10.15.0 contract:v1:write
corepack pnpm@10.15.0 set-version 1.15.0
git add packages apps HEADLESS_LINUX.md docs/linux/REMOTE_FLEET.md docs/architecture/TOOL_CONTRACT.md tests/fixtures/tool-contract-v1.json RELEASE_NOTES_v1.15.0.md package.json pnpm-lock.yaml
git commit -m "feat: add remote fleet snapshot"
```

Run the common Ubuntu gate using fake pinned SSH targets and stop before external release mutation.

**v1.15 acceptance:** one read-only request gives a deterministic, bounded health/inventory/service snapshot for up to 20 already registered hosts with truthful partial failures and no new remote authority.

**v1.15 proof:** the local mcp-server suite passes (`218` passed plus one Windows-only skip in the focused run, including the new timeout, cancellation, snapshot, audit, and schema cases), the full local root suite passes (`219` mcp-server tests; Linux-only process tests are skipped on Windows), lint/typecheck/catalog/contract/diff checks pass, and the Ubuntu 24.04 VM103 gate passes: integration `3/3`, headless packaging `7/7`, release gate `10/10`, catalog `217`, and full unit suite with capabilities `121/121` (durable-shell included), mcp-server `219/219`, and CLI `17/17`. Public-repo hygiene is verified locally; its VM copy is intentionally Git-free. DEB/tar package inspection, packaged smoke (`211` provider-filtered tools), provenance, and upgrade/rollback preflight passed. Artifacts are retained in `dist/v1.15.0-ubuntu-vm103/` with tar SHA-256 `e21cf02897f4ddb69b788b16e2b838828d240b5498984ed6fb89d395815d8fe7` and DEB SHA-256 `b073d140ed95a33bb258f8bbc26821014c7647548933ee9e8a7890846f480c03`, bound to commit `d21e96ce23acf069f30d38ab6b758d57707fb42a`. The seven-day soak remains waived and no external push/tag/release was performed.

---

### Task 5: v1.16.0 explicit additive server profiles

**Files:**
- Create: `packages/mcp-server/src/server-profile.ts`
- Create: `packages/mcp-server/src/server-profile.test.ts`
- Modify: `packages/mcp-server/src/server.ts`
- Modify: `packages/mcp-server/src/tool-registry.ts`
- Modify: `apps/cli/src/bin/headless.ts`
- Modify: `apps/cli/src/runtime/stdio-mcp-runtime.ts`
- Modify: `apps/cli/src/commands/doctor.ts`
- Modify: `HEADLESS_LINUX.md`
- Modify: `docs/architecture/TOOL_CONTRACT.md`
- Modify: `tests/fixtures/tool-contract-v1.json`
- Create: `docs/linux/SERVER_PROFILES.md`
- Create: `RELEASE_NOTES_v1.16.0.md`

- [x] **Step 1: Define profile parsing tests**

Accept only `core`, `operator`, `fleet`, or `full` from an explicit CLI flag or `BAITONGHUB_LINUX_MCP_PROFILE`. Default to `full` so existing v1 clients see the same tools; the existing Secure Tunnel permission profile remains independently enforced. Invalid values fail closed with a sanitized `INVALID_INPUT` diagnostic.

- [x] **Step 2: Implement filtering without permission changes**

Filter only `tools/list` advertisement and dispatch visibility. `core` contains workspace/file/read/Git/health/shell; `operator` adds service/package/schedule/backup/support evidence; `fleet` adds registered remote tools; `full` is the union. A profile must never bypass confirmation, registered roots, Secret Service, ownership, or audit checks. Include the selected profile in sanitized health metadata.

- [x] **Step 3: Prove compatibility**

Run the same MCP initialization and read-only workspace scenario under all four profiles. Assert that v1 schemas are unchanged, unavailable tools return `PLATFORM_UNSUPPORTED` or `CAPABILITY_UNAVAILABLE`, and a profile cannot expose Windows-only tools or remote tools without their providers wired.

- [x] **Step 4: Package and hold the v2 decision**

```bash
corepack pnpm@10.15.0 typecheck
corepack pnpm@10.15.0 test
corepack pnpm@10.15.0 docs:tools
corepack pnpm@10.15.0 contract:v1:write
corepack pnpm@10.15.0 set-version 1.16.0
git add packages apps docs HEADLESS_LINUX.md tests/fixtures/tool-contract-v1.json RELEASE_NOTES_v1.16.0.md package.json pnpm-lock.yaml
git commit -m "feat: add explicit server profiles"
```

Run the full Ubuntu/package gate for every profile. Do not create a v2 tag or change the v1 fixture.

**v1.16 acceptance:** operators can reduce the advertised surface explicitly for least surprise while the default behavior and all v1 tool schemas remain compatible.

**v1.16 proof:** local server-profile and registry tests pass (`221` mcp-server tests plus one Windows-only skip in the full local run), CLI profile wiring tests pass on Linux when enabled, full local unit/integration/packaging/release/catalog/contract/lint/typecheck/diff gates pass, and Ubuntu 24.04 VM103 passes clean-build lint/typecheck, full unit (`17` projects; capabilities `121/121`, mcp-server `222/222`, CLI `18/18`), integration `3/3`, headless packaging `7/7`, release gate `10/10`, and catalog `217`. The DEB/tar package smoke reports `211` provider-filtered tools; package inspection, provenance, and upgrade/rollback preflight pass. Artifacts are retained in `dist/v1.16.0-ubuntu-vm103/` with tar SHA-256 `efaf82b904d3ca070adfdc41e6aaaddd242e7bdcdfe68f76cf3b8c79dac8921b` and DEB SHA-256 `0c111cd1614d61b45543fbb9d928dfd9ffef12e8bfbcf4cc89457babc19dd343`, bound to commit `9f1188bcb1d91a058424a21f6b113354c4986d57`. Public-repo hygiene passes locally; the VM source copy is intentionally Git-free. The seven-day soak remains waived and no external push/tag/release was performed.

---

## Forward plan: v1.17–v1.20

These releases are intentionally additive. Each one is implemented and verified independently; the frozen v1 tool contract is regenerated only when a new tool is actually shipped.

### Task 6: v1.17.0 bounded audit query

**User value:** an operator can answer “what happened?” after an agent run without opening raw logs or exposing request arguments.

**Public shape:** add `audit_query` with `since`, `until`, `tool`, `resultCode`, `limit`, and opaque `cursor`. Return redacted event summaries (`timestamp`, `tool`, `operation`, `workspaceId` hash/alias, `resultCode`, `durationMs`) plus `nextCursor` and truncation state.

**Safety and tests:** use the existing activity store; never return command lines, paths, environment, client identity, API keys, or provider stderr. Enforce owner-isolated cursors, deterministic ordering, and a serialized 256 KiB cap. Add schema, runtime, registry, HTTP/STDIO, and redaction regression tests before implementation.

**Release gate:** full Ubuntu unit/integration/package/release/catalog/contract gates, plus a canary proving audit output cannot contain configured secret values.

**v1.17 proof:** local focused audit-query/registry/profile tests pass (`41/41`) and the audit repository integration suite passes (`4/4`); the full local workspace suite, integration, packaging, release, catalog, contract, rebrand, lint, typecheck, and diff checks pass. Ubuntu VM103 from commit `655e1c8e9d6a09f1f7890487664ae69a4898d32a` passes clean-install typecheck/lint and the full unit suite (`17` projects; capabilities `121/121`, mcp-server `226/226`, CLI `18/18`), integration `3/3`, headless packaging `7/7`, release gate `10/10`, catalog/contract checks (`218` tools), and packaged DEB/tar smoke including a structured `audit_query` response (`toolCount=212`, bounded `auditCount=10`, `auditTruncated=true`). Package inspection and upgrade/rollback preflight pass; artifacts are retained in `dist/v1.17.0-ubuntu-vm103/` with tar SHA-256 `21787bc245eb1f052c15f396772a92c6aac5f24de344cc2446a030f5c43ab87d` and DEB SHA-256 `d464dfdd946fed81e82f0198b2555f5200a8efb1b137d4038de12d600e8cb5b1`. Provenance records Ubuntu Node `v24.14.0`, Linux `x64`, and the exact commit. The seven-day soak is waived and no external push/tag/release was performed.

### Task 7: v1.18.0 workspace snapshot manifest

**User value:** compare a project before and after an agent run, or verify a release input, without reading file contents through MCP.

**Public shape:** add `workspace_snapshot` with a registered `workspaceId`, relative `path`, `maxEntries`, and `hashMode` (`none` or bounded `sha256`). Return sorted regular-file metadata (`path`, `bytes`, `mtimeMs`, optional `sha256`), counts, and an opaque continuation cursor.

**Safety and tests:** walk only registered roots; reject devices/FIFOs/sockets and symlink escapes; do not follow symlinks; bound entry count, bytes hashed, and serialized output. Add deterministic ordering, cursor ownership, race/error sanitization, and large-tree tests.

**Release gate:** Ubuntu clean-tree run with a synthetic large workspace, package smoke, and proof that no file content or unregistered path is returned.

**v1.18 proof:** local workspace-snapshot/registry tests pass (`41` mcp-server files with `230/230` tests in the full suite; the snapshot suite has `2` Windows tests passed and the Linux symlink regression enabled on Ubuntu), full local unit/integration/packaging/release/catalog/contract/rebrand/lint/typecheck/diff checks pass, and Ubuntu VM103 from commit `23e20c2c9e93776db14a6e8c97474030998c1179` passes clean-install lint/typecheck and the full unit suite (`17` projects; capabilities `121/121`, mcp-server `230/230`, CLI `18/18`), integration `3/3`, headless packaging `7/7`, release gate `10/10`, catalog/contract checks (`218` tools), and packaged DEB/tar smoke including a structured `workspace_snapshot` manifest (`toolCount=212`, `snapshotCount=10`, `snapshotTruncated=true`). Package inspection and upgrade/rollback preflight pass; artifacts are retained in `dist/v1.18.0-ubuntu-vm103/` with tar SHA-256 `e62127cbcf3f713132c3e913a2d3d64adba859e8aaf15640c1852d6e55f9bd6a` and DEB SHA-256 `153e697281f153a5494dd1a5e730d5575b015d3f0e4e41a02cd8a8cfdee17ff1`, bound to the exact commit with Ubuntu Node `v24.14.0`/Linux `x64` provenance. The seven-day soak is waived and no external push/tag/release was performed.

### Task 8: v1.19.0 reconnect-safe task events

**User value:** a client can resume progress for background shell, rollout, backup, and other durable work after an HTTP reconnect or process restart.

**Public shape:** add `task_events` with `taskId`, optional `cursor`, `limit`, and `waitMs` (bounded). Return lifecycle events (`queued`, `started`, `progress`, `log_available`, `completed`, `failed`, `cancelled`) with monotonic sequence numbers and `nextCursor`.

**Safety and tests:** persist only sanitized event metadata; enforce task ownership and workspace scope; cap event count/bytes and wait duration; never include command text, environment, secrets, or raw stderr. Test restart/replay, cursor gaps, cancellation, timeout, and concurrent readers.

**Release gate:** Ubuntu durable-process suite, HTTP reconnect integration, and package upgrade/rollback with an event stream in flight.

**v1.19 proof:** `TaskEventsService` projects owned durable shell and remote-rollout snapshots into bounded lifecycle metadata, with owner-bound cursor pagination and bounded reconnect wait. Local focused service/registry tests pass (`42/42` mcp-server tests in the focused run), full local unit, integration, packaging, release, catalog, contract, rebrand, lint, typecheck, and diff checks pass, and Ubuntu VM103 from commit `dbd3a1f9f251ea31698d2caeb1df4fc1d32e8df1` passes clean-install lint/typecheck and the full unit suite (`17` projects; capabilities `121/121`, mcp-server `235/235`, CLI `18/18`), integration `3/3`, headless packaging `7/7`, release gate `10/10`, catalog/contract checks (`219` tools), and v1 acceptance (`5/5`). Packaged DEB/tar smoke returns a completed task event stream (`taskEventCount=2`) alongside audit and workspace snapshot checks; package inspection, provenance, and upgrade/rollback preflight pass. Artifacts are retained in `dist/v1.19.0-ubuntu-vm103/` with DEB SHA-256 `5570329086107075453664a97922982869613016d1753c17a62f646cb13022dd` and tar SHA-256 `deb65c7b5bef9c43c99cc16be7399eeb602ba61ba8da9037588b7697d1416c37`, bound to the exact commit with Ubuntu Node `v24.14.0`/Linux `x64` provenance. The seven-day soak is waived and no external push/tag/release was performed.

### Forward extension after v1.20

These milestones are planning targets only until each preceding release passes
the common gate. They stay additive and Linux headless; none may widen the
registered-root, ownership, confirmation, process-group, remote-host, or
Secret Service boundaries. If evidence does not justify a milestone, keep the
existing stable tool surface and ship only security/maintenance fixes.

1. **v1.21 task history:** persist sanitized task summaries with retention and
   owner/workspace-hash filters; prove restart replay and bounded pruning.
2. **v1.22 diagnostics snapshot:** provide one deterministic read-only incident
   view; prove every section has the same redaction and availability semantics
   as its source tool.
3. **v1.23 remote drift diff:** compare two registered-host snapshots without
   arbitrary remote commands; prove no mutation path is reachable through the
   diff tool.
4. **v1.24 package verification:** verify local DEB/tar provenance and hashes
   before an operator-selected upgrade; prove failures cannot install or alter
   state.

### Task 9: v1.20.0 policy explanation

**User value:** clients receive a truthful explanation for `CAPABILITY_UNAVAILABLE`, consent, profile, dependency, or registered-root failures instead of trial-and-error retries.

**Public shape:** add `policy_explain` with `tool`, optional operation context, and `workspaceId`. Return sanitized decisions (`allowed`, `reasonCode`, `requiredProfile`, `requiresConsent`, `missingDependencies`, `registeredRootRequired`) without evaluating or executing the requested action.

**Safety and tests:** derive output from the same policy/capability registry used by dispatch; never reveal hidden tool names, filesystem topology, secrets, or bypass tokens. Add parity tests proving an explanation cannot change a subsequent authorization decision.

**Release gate:** all standard gates plus a negative test matrix for core/operator/fleet profiles and Wayland/X11/headless dependency states.

- [x] **Implementation and proof:** `policy_explain` is registered on the
  core/full headless surfaces, derives decisions from the live `ToolRegistry`,
  `DefaultPermissionEngine`, capability advertisement, and shared destructive
  inspector, and returns only sanitized reason codes. It never dispatches the
  requested tool. Focused tests cover approval, capability-unavailable,
  registered-root, hidden-tool, and confirmation paths; the registry parity
  test proves the capability provider is not called.
- [x] **v1.20 verification:** local focused tests pass (`43/43`), full local
  workspace tests pass (17 projects; mcp-server `238 passed / 2 Windows-only
  skips`, CLI `13 passed / 5 skips`), integration tests pass on Ubuntu `3/3`,
  release gate `10/10`, headless packaging smoke `7/7`, catalog/contract checks
  pass at `220` tools, and Ubuntu VM103 runs the Linux-only durable-process
  suite (`240/240` mcp-server tests, including detached-group cleanup), lint,
  typecheck, full unit, integration, acceptance (`5/5`), and package checks.
  DEB/tar provenance, forbidden-file inspection, and upgrade/rollback preflight
  pass for source commit `0887d5a7c71af9570ba42ab06f8c0765aa52316e` with Node
  `v24.14.0`/Linux `x64`. Artifacts are retained under
  `dist/v1.20.0-ubuntu-vm103/`: DEB SHA-256
  `0bfdec949f48ebfa209c827fc9af7a0a204de6618d2baec6dc057a82999575ca` and
  tar SHA-256
  `be4ccab6a48401ff0ec3545402ffa10a23524c14bfbad5ed346934ea3dc98070`.
  The VM source export was normalized to LF for `git diff --check`; public
  Git-backed hygiene remains additionally verified on the local checkout. The
  seven-day soak remains waived and no external push/tag/release was performed.

### Task 10: v1.21.0 task history

**User value:** operators can find prior owned work after a reconnect or a
runtime restart without reopening raw logs or rediscovering command metadata.

**Public shape:** add `task_history` with optional `workspaceId`, redacted
`workspaceHash`, `state`, ISO `since`/`until`, bounded `limit`, and an opaque
`cursor`. Return newest-first summaries containing only task ID, task kind,
state, timestamps, workspace hash, result code, and duration.

**Safety and tests:** derive shell entries from the owner-filtered durable list
and rollout entries from the owner-filtered rollout store. Hash workspace IDs
before returning them, reject invalid IDs/states/time ranges, bind cursors to
the authenticated owner and filter, cap retention at 500 entries and pages at
100, and fail closed when providers are absent. Add tests proving no command,
path, stdout/stderr, environment, host, credential, or provider error leaks.

**Release gate:** run the common Ubuntu unit/integration/package/release gates,
the Linux durable-process suite, catalog/contract/rebrand/diff checks, and
packaged smoke that lists shell and rollout history with filters and a
continuation cursor. Stop before external push/tag/release.

- [x] **Implementation and proof:** added the owner-filtered, redacted
  `TaskHistoryService`, `task_history` schema/tool/registry wiring, workspace
  hash projection, cursor binding, 500-entry retention and 100-entry pages,
  and packaged smoke coverage. Local focused tests pass (`45/45` in the
  mcp-server registry/history set), lint/typecheck/catalog/contract/rebrand,
  full local tests, release and packaging tests pass. Ubuntu VM103 passed the
  Linux durable-process suite with no skips, full unit tests (`capabilities
  121/121`, `mcp-server 246/246`, `CLI 18/18`), integration `3/3`, release
  gate `10/10`, v1 acceptance `5/5`, packaging tests `7/7`, and catalog/contract
  checks at `221` tools. Packaged DEB and tar smoke both returned
  `taskHistoryCount=1`; provenance, forbidden-file inspection, and
  upgrade/rollback preflight passed. Artifacts are retained under
  `dist/v1.21.0-ubuntu-vm103/`: DEB SHA-256
  `3e06ac0b6b9189dab9cd78870646d03b51365d40301811591b28e726e78eb80c` and
  tar SHA-256
  `9e7fd64cb71e4fd8b936c9f50a95b58ef53322da766e0da374e240820be0786d`.
  The seven-day soak remains waived and no external push/tag/release has been
  performed.

### Task 11: v1.22.0 diagnostics snapshot

**User value:** operators can diagnose one incident with one bounded read-only
call instead of stitching together health, runtime, audit, and dependency
queries manually.

**Public shape:** add `diagnostics_snapshot` with an empty strict input object.
Return fixed `health`, `runtime`, `audit`, and `dependencies` sections plus an
ISO `snapshotAt` and aggregate `status` (`ready`, `degraded`, or `unavailable`).

**Safety and tests:** reuse existing health/runtime/audit providers, project
only counts, booleans, bounded counters, and validated dependency names, and
never return tool names, paths, commands, topology, credentials, or provider
errors. Cap serialized output at 128 KiB; fail closed when every provider is
absent and preserve truthful degraded state when one source is unavailable.
Add provider-failure, redaction, fixed-section, and cancellation tests.

**Release gate:** common Ubuntu unit/integration/package/release gates, Linux
durable-process tests, generated catalog/contract/rebrand/diff checks, and
packaged DEB/tar smoke that verifies `diagnostics_snapshot` is present and
returns all fixed sections. Stop before external push/tag/release.

- [x] **Implementation and proof:** added the read-only
  `DiagnosticsSnapshotService` and optional registry tool. It reuses health,
  runtime-metrics, and audit providers, emits fixed redacted sections with
  truthful `ready`/`degraded`/`unavailable` status, validates dependency names,
  and enforces a 128 KiB serialized cap. Focused service/registry tests,
  lint/typecheck, full local tests, packaging, v1 contract, and catalog checks
  pass. Ubuntu VM103 passed the Linux durable-process suite with no skips, full
  unit tests (`capabilities 121/121`, `mcp-server 250/250`, `CLI 18/18`),
  integration `3/3`, release gate `10/10`, v1 acceptance `5/5`, packaging tests
  `7/7`, and catalog/contract checks at `222` tools. Packaged DEB and tar smoke
  returned `diagnosticsStatus=degraded` with all fixed sections; provenance,
  forbidden-file inspection, and upgrade/rollback preflight passed. Artifacts
  are retained under `dist/v1.22.0-ubuntu-vm103/`: DEB SHA-256
  `a744561af6def74df18258b173252ae05d5dfab27fd300b77c9e8e661be5f392` and
  tar SHA-256
  `04651a6fde56ef00cb5f75025875e7a517cc480fdeb8e64ff07f5ff2b184e1d5`.
  The seven-day soak remains waived and no external push/tag/release has been
  performed.

### Task 12: v1.23.0 remote fleet drift diff

**User value:** operators can detect configuration drift across registered
Linux hosts without manually comparing large remote snapshots or granting a
new remote execution path.

**Public shape:** add `remote_fleet_diff` with `hostIds` (1–20 registered
aliases), a bounded prior `remote_fleet` snapshot `baseline`, and
`maxParallel` (1–4). Fetch a fresh fixed-operation snapshot and return only
per-host status, changed section names (`health`, `inventory`,
`service-status`), and aggregate counts.

**Safety and tests:** validate baseline structure, host scope, duplicate IDs,
depth, and serialized size before any remote call. Delegate exclusively to
the existing `RemoteFleetRuntime` snapshot operation; reject hostname,
command, credential, and mutation fields. Never return baseline/current raw
values or provider errors. Add tests for changed/unchanged/unavailable hosts,
invalid/oversized baselines, cancellation, and no mutation path.

**Release gate:** common Ubuntu unit/integration/package/release gates, Linux
durable-process tests, generated catalog/contract/rebrand/diff checks, and
packaged smoke that verifies the tool is advertised while remote-host access
remains registry-gated. Stop before external push/tag/release.

- [x] **Implementation and proof:** added `RemoteFleetDiffService` and the
  read-only `remote_fleet_diff` tool. It validates a prior snapshot's host
  scope, duplicate IDs, depth, and 256 KiB input cap, delegates only to the
  fixed `RemoteFleetRuntime` snapshot operation, and returns changed section
  names plus sanitized status without raw remote values or provider errors.
  Focused diff/registry tests pass (`45/45`), full local tests pass (`254`
  mcp-server tests; Windows-only durable tests skip locally), and Ubuntu VM103
  passes the Linux durable suite with no skips, full unit tests (`capabilities
  121/121`, `mcp-server 254/254`, `CLI 18/18`), integration `3/3`, release gate
  `10/10`, v1 acceptance `5/5`, packaging tests `7/7`, and catalog/contract
  checks at `223` tools. Packaged DEB and tar smoke confirmed
  `remoteFleetDiffAdvertised=true`; provenance, forbidden-file inspection, and
  upgrade/rollback preflight passed. Artifacts are retained under
  `dist/v1.23.0-ubuntu-vm103/`: DEB SHA-256
  `ec3321bdc20f35abcc4fc7b83061f6926aa985b67a3bd81a5eb591ad19e0e456` and
  tar SHA-256
  `9f942e97bd6c6725061644c8e8328b397193b7dfdfa7ebff60f99422b3e82788`.
  The seven-day soak remains waived and no external push/tag/release has been
  performed.

### Task 13: v1.24.0 offline release verification

**User value:** operators can verify an already-built Linux package before an
upgrade without downloading, installing, executing, or mutating anything.

**Public shape:** add `release_verify` with an explicit registered
`workspaceId`, metadata/checksum paths, optional CycloneDX SBOM path, optional
version, and one to four `{path, sha256}` artifact entries. Return only a
boolean, normalized artifact names, version/source commit, SBOM component
count, and sanitized reason codes.

**Safety and tests:** read manifests through the existing `FileService` root
guard, delegate hashes only to `artifact_verify`, reject absolute/traversal,
duplicate, malformed, oversized, or special-file inputs, and fail closed on
metadata/checksum/SBOM mismatch or provider errors. Add no shell, network,
apt, dpkg, installer, secret, or remote authority.

**Release gate:** common Ubuntu unit/integration/package/release gates,
generated catalog/contract/rebrand/diff checks at `224` tools, and packaged
smoke that verifies advertisement. The seven-day soak remains waived.

- [x] **Implementation and proof:** `ReleaseVerifyService` and the
  read-only `release_verify` tool validate registered relative artifact paths,
  Baitonghub provenance metadata, GNU SHA-256 manifests, optional CycloneDX
  SBOMs, and delegated `artifact_verify` digests with bounded sanitized output.
  Focused service/registry tests pass (`49/49`), local lint/typecheck and full
  unit tests pass (`mcp-server 261/261`; Linux-only durable tests are run on
  Ubuntu), integration passes `3/3`, packaging tests `15/15`, release gate
  `9/9`, v1 acceptance `5/5`, and catalog/contract checks pass at `224` tools.
  Ubuntu VM103 from source commit
  `1651b29d7f51f2e83bc26aa622aca95319f5f4ad` passes rebrand, lint, typecheck,
  full unit tests (`17` projects; capabilities `121/121`, mcp-server
  `261/261`, CLI `18/18`), integration `3/3`, packaging `7/7`, release gate
  `9/9`, acceptance `5/5`, catalog/contract `224`, and `git diff --check`.
  DEB/tar package inspection, installed-package smoke, provenance, and
  upgrade/rollback preflight pass; packaged smoke reports
  `releaseVerifyAdvertised=true`. Artifacts are retained under
  `dist/v1.24.0-ubuntu-vm103/`: DEB SHA-256
  `98f54723bd36c087c9d1ee39771ed76b28b20108fbc1d77f7f8bcdbd161727be` and
  tar SHA-256
  `9760925477223010d992612680fb5b252f75de24bb0d3585bce432d9ce9530b9`.
  The seven-day soak remains waived and no external push/tag/release has been
  performed.

### Task 14: v1.25.0 environment preflight

**User value:** an operator can see whether the headless Linux runtime and its
optional providers are ready before attempting a workflow, without trial calls
or disclosure of host topology.

**Public shape:** add `environment_preflight` with an empty strict input. It
delegates only to the existing `health` `check_all` provider and returns
platform, display-server, Node major/runtime, bounded capability readiness,
consent flags, and sanitized missing-dependency names.

**Safety and tests:** project only allowlisted fields from health; never return
hostname, CPU model, absolute executable paths, environment values, command
lines, provider error text, or secrets. Cap checks and missing-dependency lists,
map provider exceptions to `CAPABILITY_UNAVAILABLE`, and return a truthful
`ready`/`degraded`/`unavailable` status. Add no new authority or system calls.

**Release gate:** common Ubuntu unit/integration/package/release gates,
generated catalog/contract/rebrand/diff checks at `225` tools, and packaged
smoke that verifies advertisement. The seven-day soak remains waived.

- [x] **Implementation and proof:** service, tool registration, CLI wiring,
  catalog/fixture, packaging smoke, docs, and Ubuntu VM103 evidence are
  complete. VM103 full gate completed with `GATE_EXIT=0`; packaged DEB/tar
  inspection and upgrade/rollback preflight passed. Packaged smoke reported
  `toolCount=219` and `environmentPreflightAdvertised=true`. Artifacts are
  retained under `dist/v1.25.0-ubuntu-vm103/`: DEB SHA-256
  `804a9094fc0f8e28d410a3e1e848ac2903b2f430d1b825b2f12afff33f6b693f` and
  Linux x64 tar SHA-256
  `129956e546d2e5a74a4b864068c4b73fadfc0d3acc4f74ea3fe526516420bea4`.
  The final source archive SHA-256 is
  `f987311c2612942f2cd493d6d86f0e49144ea3a4e040b231504bfaf621a926a7`.
  The seven-day soak remains waived. Do not push/tag/release until all gates
  pass and the user
  explicitly approves those external mutations.

---

## v2.0.0 decision gate

After v1.19, collect actual `tools/list` and error telemetry from the audit-safe aggregate (counts and result codes only). Do not start v2 implementation unless all conditions hold:

- [ ] A real client incompatibility cannot be solved by an optional field, additive tool, or profile.
- [ ] `tests/fixtures/tool-contract-v1.json` remains frozen and `tests/fixtures/tool-contract-v2.json` is generated separately.
- [ ] `docs/migrations/V1_TO_V2.md` maps every removed/renamed/behavior-changing field and includes rollback.
- [ ] Packaged acceptance runs the same scenario against the v1 compatibility profile and the proposed v2 default.
- [ ] Any historical tag collision has an explicit namespace decision.
- [ ] The user separately approves implementation and later push/tag/release mutations.

If the evidence does not satisfy every item, continue additive v1 maintenance and security fixes; do not manufacture a v2 breaking change merely to increase the version number.

## Definition of done for each minor version

- Focused TDD tests pass on Windows and Ubuntu where applicable; Linux-only process/watch tests execute on Ubuntu rather than being silently skipped.
- Full unit, integration, packaging, release-gate, catalog, v1-contract, rebrand, and `git diff --check` gates pass.
- DEB/tar package smoke passes from the exact local commit and forbidden-file/secret-canary scans are clean.
- Version, release notes, README/HEADLESS_LINUX docs, generated catalog, fixture, SBOM, provenance, and checksums agree.
- Working tree is clean and external operations remain paused until explicit human approval.

## Self-review

- Scope coverage: service logs, runtime metrics, workspace changes, remote fleet aggregation, profiles, task history, and the v2 governance gate each have concrete files, API shape, tests, packaging, and acceptance.
- Authority check: all new operations are read-only or reuse registered-host operations; no task widens filesystem, process, remote, or secret authority.
- Compatibility check: v1 remains the only frozen public contract through v1.25; v2 is conditional and never inferred from tool count.
- Operational check: the seven-day soak remains waived and is never represented as a passing production gate.
